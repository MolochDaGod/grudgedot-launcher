import { Editor } from '@/components/engine/Editor';

export default function GrudgeEnginePage() {
  return (
    <div className="h-[calc(100vh-3.5rem)] w-full overflow-hidden">
      <Editor />
    </div>
  );
}
