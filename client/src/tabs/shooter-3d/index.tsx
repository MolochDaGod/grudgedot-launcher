// Registry-listed tab for /shooter-3d. Delegates to the real page so the
// Babylon-heavy shooter code stays lazy-loaded only when this tab is visited.
// Considered part of the BabyGrudge hub (see tabs.registry.json).
export { default } from '@/pages/shooter-3d';
