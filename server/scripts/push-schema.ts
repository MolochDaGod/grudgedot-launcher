/**
 * DEPRECATED: Use `npx drizzle-kit push` for schema migrations with MySQL.
 * This script is no longer needed — Drizzle Kit handles schema push directly.
 *
 * Usage:
 *   npx drizzle-kit push
 */

console.log("⚠️  This script is deprecated.");
console.log("   Use: npx drizzle-kit push");
console.log("   Drizzle Kit will push the schema defined in shared/schema.ts to MySQL.");
process.exit(0);
